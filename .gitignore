.env
.env.*
!.env.example
__pycache__/
*.pyc
.venv/
venv/

# Python build artifacts (sdist/wheel from `python -m build`)
dist/
build/
*.egg-info/
.DS_Store
*.log
devices.json

# iOS: Voxa.xcodeproj is generated by XcodeGen (run `xcodegen generate` in ios/)
ios/Voxa.xcodeproj/
ios/build/
ios/DerivedData/
*.xcuserstate
.swiftpm/
.playwright-mcp/
billing.json
users.json
waitlist.jsonl
metrics.jsonl
events.jsonl
sessions.jsonl
actual_cost.json
attested.json
call_meter.json
last_session.json
last_session.json.tmp
deploy/secrets.local.env

.superpowers/
.worktrees/

# Node dependencies (never commit)
node_modules/

# Internal tooling output / working notes (not for the public repo)
.agents/
.workflow-audit/

# Local dev screenshots + App Store submission assets (kept out of the repo)
appstore-screenshots/
# Stray image dumps at the repo root (real assets live in web/public, assets/, static/)
/*.png
/*.jpg
/*.jpeg

# graphify (cloned tool source + generated knowledge graph)
/graphify/
/graphify-out/
